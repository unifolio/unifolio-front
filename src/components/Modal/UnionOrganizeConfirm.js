import React from 'react'
import styled from 'styled-components';

const UnionOrganizeConfirmModal = () => {
  return (
    <ModalLayout>
      UnionOrganizeConfirm
    </ModalLayout>
  )
}

const ModalLayout = styled.div`

`

export default UnionOrganizeConfirmModal;